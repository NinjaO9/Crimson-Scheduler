{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --include=dev",
  "buildCommand": "npm run build && python -m pip install -r requirements.txt && python web/manage.py collectstatic --noinput"
}
